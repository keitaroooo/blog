# インフラ作業ログ

## 方針

- **デプロイ設定**: `node/app/netlify.toml` で管理。Terraform は使用しない。
- **DNS 管理**: [`keitaro-yamaguchi`](https://github.com/keitaroooo/keitaro-yamaguchi) リポジトリに集約。

## Netlify

- **Site ID**: `c608a141-5fa6-4f1c-8884-8688484c9895`
- **ビルド設定**: `node/app/netlify.toml` に定義済み（`npm run export` → `__sapper__/export`）
- **ドメイン**: `blog.keitaroooo.com`（Netlify ダッシュボードで設定済み）

### Terraform を使わない理由

`netlify.toml` でビルド設定を管理でき、ドメインもダッシュボードで設定済みのため、Terraform で管理するリソースがない。
